# Benchmark cache

This directory is reserved for optional downloaded public corpora.

The artifact smoke path does not require network access and does not require files in this directory. Full public-corpus runs may populate this directory or `examples/public-corpus/downloads/` with pinned sources.
