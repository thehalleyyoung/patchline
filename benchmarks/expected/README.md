# Expected benchmark outputs

Future full benchmark runs should place frozen expected outputs here. The current artifact smoke path writes generated outputs to `results/generated/` instead of committing large result files.
