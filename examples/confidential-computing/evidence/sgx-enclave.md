SGX verifier evidence for the private Rails migration corpus.

The verifier report accepts the gramine runtime image digest and measurement for `sgx-private-runner`; key release is permitted only when the quote measurement matches the allowlist.
