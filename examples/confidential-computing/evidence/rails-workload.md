Rails private migration workload evidence.

The enclave replay consumes only the encrypted Rails migration corpus bundle, emits a redacted aggregate public summary, retains row-level findings as private output, and records a deterministic replay witness.
