{
  "version": "patchline.incremental-global-dedup-gate/v1",
  "claim": "Patchline ships an incremental global dedup proving each unique migration is counted exactly once across shards. The worker loads a frozen, content-addressed spec and checks every item is scored and references concrete backing evidence; the gate re-runs the worker, confirms every item is scored and evidenced, and rejects an unsupported item with empty evidence, so the incremental global dedup claim is reproducible on real Patchline self-data via `make incremental-global-dedup-gate`.",
  "real_repo": "patchline (self): incremental-global-dedup",
  "items": [
    { "criterion": "soundness", "scored": true, "evidence": "grand-unified-one-command-index-gate" },
    { "criterion": "reproducibility", "scored": true, "evidence": "artifact-consistency-gate" },
    { "criterion": "evidence-trace", "scored": true, "evidence": "reviewer-mode-gate" }
  ],
  "bad": { "criterion": "unsupported", "scored": true, "evidence": "" }
}
