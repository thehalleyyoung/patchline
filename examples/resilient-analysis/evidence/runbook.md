# Resilient analysis runbook

The June corpus sweep ran three deterministic shards across three workers. Operators injected a worker loss, an availability-zone partition, and a stale cache artifact before accepting the final run.
