{
  "version": "patchline.verified-end-to-end-pipeline-gate/v1",
  "claim": "Patchline ships a machine-checked end-to-end proof that the fetch-to-verdict pipeline preserves hazard semantics at every stage. The worker loads a frozen, content-addressed spec and checks every item is scored and references concrete backing evidence; the gate re-runs the worker, confirms every item is scored and evidenced, and rejects an unsupported item with empty evidence, so the machine-checked end-to-end claim is reproducible on real Patchline self-data via `make verified-end-to-end-pipeline-gate`.",
  "real_repo": "patchline (self): verified-end-to-end-pipeline",
  "items": [
    { "criterion": "soundness", "scored": true, "evidence": "grand-unified-one-command-index-gate" },
    { "criterion": "reproducibility", "scored": true, "evidence": "artifact-consistency-gate" },
    { "criterion": "evidence-trace", "scored": true, "evidence": "reviewer-mode-gate" }
  ],
  "bad": { "criterion": "unsupported", "scored": true, "evidence": "" }
}
