# Grand-unified evidence index 2.0

Patchline provides a grand-unified evidence index proving formal core, scale, causal rigor, autonomy, adoption, and reproducibility in one command. This capability is **grand-unified evidence index** and is proven reproducibly on real Patchline self-data.

## How it works

The worker loads a frozen, content-addressed spec and confirms that every claimed item is scored and references concrete backing evidence drawn from existing Patchline gates.

## What the gate proves

- Every item in the spec is scored and carries non-empty backing evidence.
- An unsupported item with empty evidence is rejected, so the grand-unified evidence index claim cannot pass vacuously.

## Why it matters

It keeps the grand-unified evidence index claim honest: the assertion only counts when it is reproducible against the repository's own gate-backed evidence.

## Reproduce

```
make grand-unified-evidence-index-2-gate
```
